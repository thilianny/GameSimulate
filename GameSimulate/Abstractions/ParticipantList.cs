using System.Collections;
using System.Collections.Generic;
using System.Collections.ObjectModel;

namespace GameSimulate.Abstractions
{
    public class ParticipantList : List<Participant>
    {
       // protected readonly List<Participant> _participants = new List<Participant>();
        
       /* public ReadOnlyCollection<Participant> Participants => _participants.AsReadOnly();

        protected ParticipantList()
        {
            _participants = new List<Participant>();
        }*/

       public ParticipantList()
       {
       }

}